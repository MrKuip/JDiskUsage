package org.kku.jdiskusage.ui.common;

import org.kku.fonticons.ui.FxIcon;
import org.kku.fonticons.ui.FxIcon.IconSize;
import javafx.concurrent.Task;

public class Notifications
{
  /*
  private List<MessageListenerIF> mi_messageListenerList = new ArrayList<>();

  public Notifications()
  {
  }

  addEventHandler(MessageListenerIF ml)
  {
    mi_messageListenerList.add(messageListener);
  }

  public void addMessage(String title, String text)
  {
     new MessageItemView(new FxIcon("information").size(IconSize.LARGE).getImageView(), title, text, 10));
  }

  record Message(String title, String message, String iconName, int timeToLive) {
  }
  */
}